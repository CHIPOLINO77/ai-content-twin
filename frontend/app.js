const API=localStorage.getItem("CONTENT_TWIN_API")||"http://localhost:8000";
const $=id=>document.getElementById(id);
async function health(){try{const r=await fetch(API+"/health");const d=await r.json();$("apiStatus").textContent=d.polza_configured&&d.model_configured?"Polza AI подключён":"Backend online • настрой .env"}catch(e){$("apiStatus").textContent="Backend offline"}}
async function upload(file){const row=document.createElement("div");row.className="uploadrow";row.textContent=file.name+" — загрузка…";$("uploads").append(row);const fd=new FormData();fd.append("file",file);try{const r=await fetch(API+"/api/upload",{method:"POST",body:fd});const d=await r.json();row.textContent=d.filename+" ✓"}catch(e){row.textContent=file.name+" — ошибка подключения"}}
$("pick").onclick=()=>$("fileInput").click();$("fileInput").onchange=e=>[...e.target.files].forEach(upload);$("dropzone").ondragover=e=>e.preventDefault();$("dropzone").ondrop=e=>{e.preventDefault();[...e.dataTransfer.files].filter(f=>f.type.startsWith("video/")).forEach(upload)};
$("generate").onclick=async()=>{const topic=$("topic").value.trim();if(!topic)return;$("ideas").innerHTML="<p>Polza генерирует идеи…</p>";try{const r=await fetch(API+"/api/ideas",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({topic:topic,count:10,platform:"shorts"})});const data=await r.json();const list=Array.isArray(data)?data:[data];$("ideas").innerHTML=list.map((x,i)=>"<div class='idea'><h3>"+(i+1)+". "+esc(x.title||"Идея")+"</h3><p><b>Hook:</b> "+esc(x.hook||"—")+"</p><p>"+esc(x.angle||"")+"</p><span class='tag'>"+esc(x.why_it_fits||"AI Content Twin")+"</span></div>").join("")}catch(e){$("ideas").innerHTML="<p>Не удалось подключиться к backend.</p>"}};
function esc(v){return String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",""":"&quot;","'":"&#039;"}[c]))}health();